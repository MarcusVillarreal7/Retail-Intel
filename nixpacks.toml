[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd dashboard && npm install"]

[phases.build]
cmds = ["cd dashboard && npm run build"]

[start]
cmd = "npx --yes serve dashboard/dist -s -l 3000"
